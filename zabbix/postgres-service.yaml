apiVersion: v1
kind: Service
metadata:
  name: zabbix-postgres
  labels:
    app: zabbix-postgres
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: zabbix-postgres
