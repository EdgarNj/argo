apiVersion: v1
kind: Service

metadata:
  name: k8s-web-hello
  
spec:
  type: LoadBalancer
  selector:
  
  
    app: k8s-web-hello

  ports:
  
  - port: 3001
    targetPort: 3000
    name: "first"
  - port: 3002
    name: "seccond"
    targetPort: 3000  
